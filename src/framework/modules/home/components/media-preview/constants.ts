import { getScaleWidth, UI_SIZES } from '~/framework/components/constants';

export const PREVIEW_MEDIA = 2;

export const MEDIA_HEIGHT = getScaleWidth(64);

export const SINGLE_MEDIA_WIDTH = getScaleWidth(102);
export const MORE_MEDIA_WIDTH = getScaleWidth(52);

export const MEDIA_GAP = UI_SIZES.spacing.minor;

export const PLAY_ICON_SIZE = getScaleWidth(24);
