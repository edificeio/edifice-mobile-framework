/**
 * BuildConfig
 * Read values from build.gradle and Info.plist files about build config constants.
 */

import { BuildConfig } from './types';

export default {
  BundleVersionOverride: 'Tutu',
  BundleVersionType: 'Tata',
  CFBundleIdentifier: 'com.ode.mobile',
} as BuildConfig;
