import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  pictureButton: {
    paddingHorizontal: 0,
    paddingVertical: 0,
    borderWidth: 0,
    backgroundColor: undefined,
  },
});
