import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  pictureButton: {
    backgroundColor: undefined,
    borderWidth: 0,
    paddingHorizontal: 0,
    paddingVertical: 0,
  },
});
