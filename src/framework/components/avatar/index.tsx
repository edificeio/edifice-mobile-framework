export * from './single';
export type { SingleAvatarProps } from './types';

/**
 * New avatar implementation
 *
 * Some feature are also missing
 * - avatar showing svg image (for single avatar)
 * - avatar showing group image (for single avatar)
 * - grayscale avatar (for avatar wrapper)
 * - badge avatar (for avatar wrapper)
 * - circled avatar (for avatar wrapper)
 * - avatar with spinner (for avatar wrapper)
 */
