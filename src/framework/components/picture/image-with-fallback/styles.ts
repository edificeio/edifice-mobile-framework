import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  imageLoader: {
    height: '100%',
    width: '100%',
  },
  imageLoaderWrapper: {
    position: 'absolute',
  },
  moduleImage: {
    alignItems: 'center',
    justifyContent: 'center',
    overflow: 'hidden',
  },
});
